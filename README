This is a port of my LogicCircuit program to the rust language, mostly as a way of learning rust.

LogicCircuit is a logic gate simulator, that models a fully-clocked system, with no propagation delays (so exceptionally simplistic, but simple to model)
